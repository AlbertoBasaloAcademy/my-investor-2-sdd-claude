## Arquitectura de software:
- valida en el server los datos, 
- guarda con una transaccion que garantize las plazas. 
- usa `hybernate` en el server y `redux` en local
- usa POO en java y funcional en ts 
- el tratamiento de errores es con try catch

<example>

```js
let x = 1;
```
</example>