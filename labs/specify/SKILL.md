---
name: specify
description: "A skill that allows the user to specify a task or request in more detail."
user-invocable: true
disable-model-invocation: true
---

# Generate a spec file

- Ask the user for the requirements
- Generate a spec using the template provided in `spec.template.md`
- Save the spec file in the `.product/specss/{slug}.spec.md` 